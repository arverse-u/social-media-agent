
import React from 'react';
import Layout from '@/components/layout/Layout';
import MediaBuffer from '@/components/media/MediaBuffer';

const Media = () => {
  return (
    <Layout>
      <MediaBuffer />
    </Layout>
  );
};

export default Media;
